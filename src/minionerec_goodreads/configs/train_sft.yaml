# @package _global_

defaults:
  - data: sft
  - model: sft_qlora
  - callbacks: sft
  - logger: csv # TODO
  - trainer: gpu
  - paths: default
  - extras: default
  - hydra: default
  - optional local: default

  - experiment: null
  - debug: null
  - _self_

trainer:
  max_epochs: 12
  log_every_n_steps: 10

data:
  batch_size: 4
  max_length: 1744 # TODO
  max_train_samples: null # debug
  max_valid_samples: null # debug

task_name: "train_sft"
tags: ["sft", "goodreads"]

train: True
test: False
ckpt_path: null

seed: 728
optimized_metric: "val/rec_MRR@10" # TODO
